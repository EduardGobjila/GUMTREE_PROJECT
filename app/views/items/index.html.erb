<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>

<body>
  <div class="wrapper">
   <div class="body-div">
    <h1>Start Exploring The Market</h1>
   </div>

   <div class="index-div">
   <%# make login to show total ads and category name %>
    <p><strong>Search in: <%= @items.length %> ads</p>
      <div>
      <%# make link to sort the items by price, location, most recent etc %>
        <%=link_to "Filter by", "#", class: "filter-btn" %>
      </div>
   </div>

    <%# add image location source%>

    <% @items.each do |item| %>
      <div class="card-product">

       <% if item.photos.attached? %>
      <% item.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>
      <% else %>
       <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
        <% end %>
            <div class="card-product-infos">
              <h2><%= item.name%></h2>
              <p><%= item.description %></p>
              <p><%= item.price %></p>
              <%= link_to 'See More', item, class: 'btn btn-danger text-white'%>
            </div>

      <%# implementing photo display -- RORY%>


      </div>
    <% end %>
    <%# this is an example only - can be deleted later %>
    <div class="card-product">
    <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
      <div class="card-product-infos">
        <h2>Product name</h2>
        <p>Product description with <strong>relevant info</strong> only.</p>
      </div>
    </div>


  </div>
</body>
</html>
